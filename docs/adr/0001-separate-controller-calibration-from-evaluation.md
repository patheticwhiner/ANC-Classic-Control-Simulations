# 控制器校准与最终评价严格分离

本仓库的跨算法比较采用共享校准集为每种控制器独立搜索参数，再在未参与调参的共享评价集上报告结果。允许针对 T1、T2、T3 信号类别分别设计控制器，但每套控制器只能使用对应类别的校准子集，并在同类的留出评价信号上冻结测试。选择该方案是为了允许不同算法和不同扰动类别使用适合自身结构的参数空间，同时避免直接针对最终评价波形调参造成的过拟合和不公平比较；稳定性与控制量限制在两阶段保持一致。
