# 全局工作计划

> 目标: 在理解被控对象性能上限的基础上，用同一 ARMAX 模型补全 demo1–4 的控制器设计实验，积累实证依据后决定研究方向。
> 创建: 2026-06-13

---

## 背景

当前有四个控制器设计 demo，分别探索不同的固定+自适应方案：

| Demo | 固定部分 | 自适应部分 | 被控对象 | 状态 |
|:---|:---|:---|:---|:---|
| demo1 RST | 极点配置 R₀/S₀ | Youla-Q (RLS, nQ=2) | 二阶教学模型 | 代码已有，需适配 ARMAX |
| demo2 LQG | LQR+Kalman | 无 | 合成 BPF 模型 | 需适配 ARMAX |
| demo3 Jafari | F(z)+FIR(N=120) | NLMS (N=64) | ARMAX(30,30,30,22) | ✅ 23+22 dB |
| demo4 ε-MOPSO | ZF 积分→Bezout→RST | 无 | ARMAX (尝试) | ❌ 对 ARMAX 已弃用 |

**核心问题**: 如何在 Youla 参数化（demo1）的"闭环极点固定"局限性和 Jafari 框架（demo3）的"手动 FIR 设计"之间，找到一个既有理论贡献又有工程价值的改进方向？

---

## 研究方向的决策框架

三条候选方向，决策将基于实证数据：

| 方向 | 核心改进 | 理论贡献 | 工程价值 | 风险 |
|:---|:---|:---|:---|:---|
| A: 自动 R₀/S₀ | 优化替代手动极点配置 | 低 (fixed-structure 优化已有成熟文献) | 中 | 低 |
| B1: 层次化自适应 | 外层重设 R₀/S₀ + 内层 Q 自适应 | 中高 (切换稳定性分析) | 高 (自动化跨管道迁移) | 中 |
| C: 完善 Jafari | 自动 FIR 设计 + 严格对比 demo1 | 中 (对比分析 + 自动化) | 高 (已有 23+22dB 基础) | 低 |

**决定方式**: 等 Phase 1–3 完成后，基于实测数据选择。

---

## Phase 1: 被控对象性能上限分析 ✅ 已完成

**目标**: 回答"ARMAX(30,30,30,22) 声学管道模型理论上最多能实现多好的噪声抑制？"

**输出**:
- `dataset/analyze_plant_performance.m` — 分析脚本
- `dataset/About_Plant_Performance.md` — 分析报告 (datasheet 格式)
- `dataset/output/plant_analysis.mat` — 量化结果

**关键发现**:
| 约束 | 结论 |
|:---|:---|
| Bode 灵敏度积分 | = 0 (无开环不稳定极点)，不限制单频抑制深度 |
| NMP 零点 Poisson 约束 | 9 个零点约束强度 0.005–0.212，近乎空洞 |
| 延迟 (458 μs) | **唯一硬约束**，反馈带宽上限 ~727 Hz |
| 前馈可能性 | 若参考信号可用，前馈不受 Bode/延迟约束 |

---

## Phase 2: 统一测试基准 (⏳ 待启动)

**目标**: 建立所有 demo 共享的测试信号和评估指标。

### 2.1 测试信号

| 编号 | 类型 | 参数 | 验证目标 |
|:---|:---|:---|:---|
| T1 | 固定频率正弦 | 334 Hz (第一共振峰) | 稳态抑制能力 |
| T2 | 线性扫频 | 300→500 Hz, 10s | 固定控制器频率鲁棒性 |
| T3 | 带限白噪声 | 100–800 Hz, 频域法+升余弦窗 | 宽带噪声综合性能（含延迟不可控区） |

**统一参数**: `Tsim=10s`, `rng(42)`, **RMS(d)=0.8**（信号进入被控对象前按 RMS 归一化）。

### 2.2 评估指标

| 指标 | 定义 | 适用测试 |
|:---|:---|:---|
| 稳态抑制 | 20·log₁₀(RMS(y_open, [3,Tsim]后80%) / RMS(y_closed, [3,Tsim]后80%)) | T1, T3 |
| 收敛时间 | 从启动到抑制 >50% 稳态值的时间，固定控制器=0 | T1, T3 |
| 频率鲁棒性 | T2 按 20Hz 分箱（300–320, 320–340, ..., 480–500），每箱局部抑制 | T2 |
| 控制能量 | max|u| 和 RMS(u) | T1, T2, T3 |

### 2.3 接口约定

各 demo 通过 `signals.model` → `DataManager` 自行加载模型。控制器函数签名：

```matlab
function result = run_demoX_test(signals, test_name, variant, params)
```

- `signals`: Phase 2 生成的 struct（含 `.model`, `.fs`, `.T1/.T2/.T3`）
- `test_name`: `'T1'` / `'T2'` / `'T3'`
- `variant`: `'fixed'` / `'adaptive'`
- `params`: demo 专用参数 struct
- `result`: 标准化 struct（字段见 `tests/controller_interface.m`）

### 2.4 输出文件

```
dataset/test_signals_armax30303022.mat  # T1+T2+T3 + y_open + meta
tests/
├── generate_test_signals.m             # 信号生成脚本（参数化模型源）
├── compute_metrics.m                   # 统一指标计算
├── controller_interface.m              # 接口规范 + init_result() 模板
├── README.md
└── output/                             # Phase 3 各 demo 结果
```

---

## Phase 3: demo1–4 统一模型实验 (待启动)

### 3.1 demo1: Youla 极点配置 + 自适应 Q

| 步骤 | 内容 | 预计耗时 |
|:---|:---|:---|
| 3.1.1 | 将 Carmona2000 的 R₀/S₀ 设计适配 ARMAX 模型 | 0.5d |
| 3.1.2 | 运行 RLS 自适应 Q 对 T1/T2/T3 | 0.5d |
| 3.1.3 | 记录: 固定部分抑制、自适应增量 (dB)、收敛时间 | — |
| 3.1.4 | 分析: Q (nQ=2) 的容量限制在 T2/T3 下的表现 | — |

**状态**: `demo1_RST/Landau2005.m` 已有代码框架，需适配。

### 3.2 demo2: LQG

#### 3.2a 固定 LQG

| 步骤 | 内容 | 预计耗时 |
|:---|:---|:---|
| 3.2.1 | ARMAX → 能观标准型 (已有 `analyze_armax_model.m` 输出) | 0.25d |
| 3.2.2 | LQR 权重 Q/R 针对 T1 手工调试 | 0.5d |
| 3.2.3 | Kalman 滤波器设计 + LQG 组装 | 0.5d |
| 3.2.4 | 对 T1/T2/T3 测试 (固定控制器) | 0.25d |

#### 3.2b 自适应 LQG (框架就位，收敛受限)

| 步骤 | 内容 | 预计耗时 | 状态 |
|:---|:---|:---|:---|
| 3.2.5 | 在 LQG 闭环上挂载 Youla-Q 自适应层 | 0.5d | ✅ 框架完成 |
| 3.2.6 | T1/T2/T3 测试 | 0.25d | ✅ 5.2/4.1/−0.8 dB |
| 3.2.7 | **诊断 Q 不收敛根因** | — | ✅ 见下文 |

**结果**: 自适应增量 ΔT1=+0.4, ΔT2=−0.8, ΔT3=−0.1 dB——Q 无实质贡献。

**根因** (详见 `memory/q-convergence-conditions.md`):
1. **残差不纯**: 裸辨识 Kalman 增益 `Kf` 产生的残差混杂未建模动态
2. **观测器不合格**: Kf 对白噪声最优而非对正弦/扫频扰动最优
3. **Q 容量不足**: nQ=6 @ 48kHz ↔ FIR 跨度 125μs << 334Hz 周期 3ms

**改进路径**: 构建增广扰动模型 → 重做 LQG → 增大 nQ → 验证收敛。
**关键参考**: 钱梵梵 (2022) — 增广状态包含成形滤波器 Σ_ω2。

### 3.3 demo3: Jafari F(z) + FIR + NLMS

| 步骤 | 内容 | 预计耗时 |
|:---|:---|:---|
| 3.3.1 | 运行现有代码对 T1/T2/T3 | 0.25d |
| 3.3.2 | f₀ 敏感度扫描: 在 334Hz±5%,±10%,±20%,±30% 处测试固定 FIR | 0.25d |
| 3.3.3 | 记录: 固定抑制、自适应增量、f₀ 敏感度曲线 | — |

**状态**: `JafariANC_RealAcoustic.m` 代码完整，需增加敏感度扫描。

### 3.4 demo4: ε-MOPSO RST（降阶路径）

**目标**: 对完整 ARMAX 模型做自动 RST 设计，但 ZF 框架无法直接处理 30 阶模型。
**策略**: `balred` 降阶 ARMAX 30→6~8 阶 → 在降阶模型上运行 ε-MOPSO RST 优化 → 用**原始 30 阶** ARMAX 仿真验证闭环性能。

| 步骤 | 内容 | 预计耗时 |
|:---|:---|:---|
| 3.4.1 | RST_toy 确认框架在二阶教学模型上正常 | 0.1d |
| 3.4.2 | ARMAX balred 降阶 (30→6~8)，评估降阶保真度 | 0.3d |
| 3.4.3 | 在降阶模型上运行 RST 优化 + 原始模型闭环验证 | 0.4d |
| 3.4.4 | 与 demo1 手动 R₀/S₀ 设计对比 | 0.1d |

**状态**: RST_armax（全阶 ZF）已证实失败；降阶路径待验证。

---

## Phase 4: 综合对比与方向决策 (待启动)

### 4.1 实验结果汇总

| Demo | 固定部分 | 自适应部分 | T1抑制 | T2近设计点 | T2远端 | T3抑制 | 收敛 | 手动步骤 |
|:---|:---|:---|:---|:---|:---|:---|:---|:---|
| 1 固定 | R₀/S₀ | 无 | ? | ? | ? | ? | 0 | P_D,H_R,H_S |
| 1 自适应 | R₀/S₀ | Q-RLS | ? | ? | ? | ? | ? | 同上 |
| 2a 固定 | LQG | 无 | ? | ? | ? | ? | 0 | Q,R 权重 |
| 2b 自适应 | LQG | Q-RLS | ? | ? | ? | ? | ? | 同上 (新增) |
| 3 固定 | F(z)+FIR | 无 | ? | ? | ? | ? | 0 | f₀, N_fir |
| 3 自适应 | F(z)+FIR | NLMS | 23dB | ? | ? | ? | ? | 同上 |
| 4 (降阶) | RST自动 | 无 | ? | ? | ? | ? | 0 | 降阶阶数 |

*T2近设计点 = 320–340 Hz 分箱, T2远端 = 480–500 Hz 分箱*

### 4.2 决策准则

- 若方向 C (demo3) 在所有测试中显著优于方向 A (demo1 固定) → 确认 Jafari 路线
- 若 demo1 自适应 (Q-RLS) 与 demo3 自适应 (NLMS) 差距主要来自 Q 阶数 → 方向 B1 可尝试增大 nQ
- 若降阶模型 (demo4) 的自动 RST 接近 demo1 手动 R₀/S₀ → 方向 A 有发表价值
- 若所有反馈方案在 T3 (宽带) 均不理想 → 调研前馈方案

---

## 时间估算

| Phase | 内容 | 预估 | 依赖 |
|:---|:---|:---|:---|
| 1 | 被控对象分析 | ✅ 完成 | — |
| 2 | 测试基准 | 0.5d | Phase 1 |
| 3.1 | demo1 适配+测试 | 1d | Phase 2 |
| 3.2a | demo2 固定 LQG | 1.5d | Phase 2 |
| 3.2b | demo2 自适应 LQG | 0.75d | Phase 3.2a |
| 3.3 | demo3 补充测试 | 0.5d | Phase 2 |
| 3.4 | demo4 降阶+RST | 0.9d | Phase 2 |
| 4 | 对比+决策 | 1d | Phase 3 |
| **总计** | | **~6d 剩余** | |

---

## 相关文件索引

| 文件 | 用途 |
|:---|:---|
| `plans/Global_Work_Plan.md` | 本文件 |
| `dataset/About_Plant_Performance.md` | 被控对象性能上限分析报告 |
| `dataset/analyze_plant_performance.m` | 性能分析脚本 |
| `memory/research-directions.md` | 三条方向的详细优劣分析 (AI 上下文) |
| `memory/work-plan.md` | 旧版工作计划 (AI 上下文, 将被本文件取代) |
